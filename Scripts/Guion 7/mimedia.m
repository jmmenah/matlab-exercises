function y = media(x)
% Función que calcula la media de los valores de un vector%Argumentos de entrada:
% x: vector de entrada%Argumentos de salida:
% y: media de los valores de x
    y = sum(x)/length(x); 
end